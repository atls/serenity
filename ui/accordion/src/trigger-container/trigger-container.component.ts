import styled         from '@emotion/styled'

import { Row }        from '@ui/layout'

import { baseStyles } from './trigger-container.styles'

const TriggerContainer = styled(Row)(baseStyles)

export { TriggerContainer }
