import { motion }     from 'framer-motion'

import styled         from '@emotion/styled'

import { baseStyles } from './content-container.styles.js'

const ContentContainer = styled(motion.div)(baseStyles)

export { ContentContainer }
