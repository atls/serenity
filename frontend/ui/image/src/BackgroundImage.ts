import styled                                from '@emotion/styled'
import { background, layout, space, system } from 'styled-system'

export const BackgroundImage = styled.div(
  system({
    cursor: true,
  }),
  layout,
  space,
  background,
)
