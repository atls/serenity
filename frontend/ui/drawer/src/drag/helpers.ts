export const isDirectionTop = direction => direction === 'top'

export const isDirectionBottom = direction => direction === 'bottom'

export const isDirectionLeft = direction => direction === 'left'

export const isDirectionRight = direction => direction === 'right'

export const isClientSide = () => typeof window !== 'undefined'
